# Flutter / Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
.flutter_settings

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# Generated platform folders are regenerated by `flutter create .`
# (you may keep them in git after the first `flutter create .` if desired)

# Android upload signing — NEVER commit the keystore or its password file.
# Anyone with these can publish updates to this app on Google Play.
android/key.properties
android/app/key.properties
*.jks
*.keystore
